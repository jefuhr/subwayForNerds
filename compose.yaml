services:
  subway:
    build:
      context: .
      args:
        APP_BASE: /subwaysForNerds/
    restart: unless-stopped
    ports:
      - "127.0.0.1:8091:8091"
    volumes:
      - subway-state:/app/state
    environment:
      APP_BASE: /subwaysForNerds/
volumes:
  subway-state:
